import sys
import math

mountain_range = [] *
# while True:
#     lastheight = 0
#     for i in range(8):
#         currheight = int(input())
#         if currheight > lastheight:
#             mountain_range.append(currheight)
#         lastheight = currheight

        #mountain_h = int(input())  # represents the height of one mountain.
        # mountain_range.append(input())

    #for m in mountain_range:


    print(mountain_range, file=sys.stderr, flush=True)
    break
    # Write an action using print
    # To debug: print("Debug messages...", file=sys.stderr, flush=True)

    # The index of the mountain to fire on.
    #print("4")
